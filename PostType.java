package ash.laces;

/**
 * Created by Ash on 3/1/17.
 *
 * - These are the two types of posts that can be made.
 *   Not sure if it's easier to just have a boolean; 1 means shoe, 0 means hat?
 *   But this is here just in case.
 */

public enum PostType {
    shoe,
    hat,
}
